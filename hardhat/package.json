{
  "name": "hardhat-project",
  "scripts": {
    "fork": "hardhat node --show-stack-traces",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "hardhat": "^2.22.19",
    "viem": "^2.23.10"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5"
  }
}
